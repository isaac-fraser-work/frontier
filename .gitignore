# The archive this repo was built from — the harness's private working copy.
# Its contents ship as skills/ and template/; a second copy would be a fork.
archive/

# Machine-local Claude Code state. settings.json is deliberately not tracked
# here: this repo is a plugin, and a plugin does not carry a project's settings.
.claude/

# Editor / shell config that lives at this project root in the author's sandbox.
.idea
.vscode
.mcp.json
.ripgreprc
.gitconfig
.gitmodules
.bashrc
.bash_profile
.zshrc
.zprofile
.profile

__pycache__/
*.py[cod]
.venv/
